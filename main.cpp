#include <fstream>

int main() {
    std::ifstream in("area.in");
    std::ofstream out("area.out");
    int n;
    in>>n;
    int points[n][2];
    for (int i = 0; i < n; ++i) {
        in>>
    }
    return 0;
}
